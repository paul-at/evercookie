(function(){
	function setCookie(){
		var ec = new <%= Evercookie.js_class %>();
		ec.set('<%= @data[:key] || @data['key'] -%>', '<%= @data[:value] || @data['value'] -%>')	
	};
	if(typeof jQuery != 'undefined') {
		jQuery(document).ready(setCookie);
	}
	else {
		setCookie();
	};
})();
